# Empty Documents YAML Test File
# Contains empty documents with various separators and comments

---
# This is the first document separator with just a comment


---


# Second document separator with empty content


---

   

# Third document with just whitespace

---

# Valid document in the middle
apiVersion: hhnet.githedgehog.com/v1alpha1
kind: VPC
metadata:
  name: valid-vpc-among-empty
  namespace: default
  labels:
    test-case: empty-documents
spec:
  description: "Valid VPC surrounded by empty documents"
  subnet: "10.0.0.0/24"

---

# Another empty document after valid one

---
# Document with only comment

---

# Document with YAML comment syntax but no actual content
# key: value (commented out)
# metadata:
#   name: commented-vpc

---

apiVersion: hhnet.githedgehog.com/v1alpha1
kind: External
metadata:
  name: valid-external-among-empty
  namespace: default
spec:
  description: "Valid External among empty documents"
  vlan: 200

---

# Final empty document


---